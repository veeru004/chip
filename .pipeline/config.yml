---
general:
  buildTool: "mta"
steps:
  mtaBuild:
    platform: 'CF'
  cloudFoundryDeploy: true
    deployTool: 'mtaDeployPlugin'
    deployType: 'standard'
    cfApiEndpoint: "https://api.cf.us10.hana.ondemand.com/"
    cfOrg: "cf02965btrial"
    cfSpace: "trialspace"
    cfCredentialsId: "cf"
